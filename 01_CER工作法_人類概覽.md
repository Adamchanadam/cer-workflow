# CER 工作法：人類概覽

CER 工作法適合長期、多批、容易中斷，或需要獨立反證的工作。它的核心是讓
Controller、同一位持久 Executor 與按風險建立的 Reviewer 分工閉環，不是規定
專案必須採用哪套文件。

本專案保留兩套檔案面向：`skills/cer-workflow/` 是可安裝 CER Skill；
根目錄 `01_CER工作法_人類概覽.md` 和 `02_CER工作法_AI執行協議.md` 是給人類
及無 Skill 環境使用的 Markdown。兩套檔可以同時存在，但規則必須一致：
01 只講人類理解，02 承載可攜 AI 協議，CER Skill 承載可安裝 runtime；不得
把三者手動改成互相打架的版本。

## CER Core v1

第一版只支援 CER 獨立模式，不依賴或操作 Agent Handoff Kit。v2 是獨立
Adapter 設計邊界，不混入 v1 runtime。

CER 可在全新或進行中的專案即叫即用。它先沿用目標專案已有需求、計劃、Issue、
Roadmap 及進度真源，不會因啟動 CER 而建立固定五份文件或平行治理。

## 三個角色

- Controller（C）是使用者主 task 的主控角色，負責全局分析、分工、讀回、裁決和使用者溝通；C 不寫 workspace。
- Executor（E1）是同一任務持續重用、可再次派工的獨立 task，也是唯一 writer；不能用一次性臨時 subagent 代替。
- Reviewer（R）只在高風險、核心承諾或 C 不能可靠反證時 fresh 建立；R 唯讀、有界，不做例行儀式。

E2 只在舊 E1 已確認停止寫入後接管，不能形成平行 writer。

## 跨 task 防錯

E1／R 有獨立上下文，不會自動繼承 C 的對話，因此每個派工包必須 self-contained。

建立 task 或開始實際驗證前，C 必須以當前可用工具證明身份來源、必要參數、
發送路徑、接收者、回傳座標與裁決點均可用。任一環缺失便停止該委派架構；
不能用文件審閱、fork 成功、單向 send、事後讀取或猜測代替通訊驗證。

新建或識別 task／thread 時，角色要能被人和工具分辨：C 用 `C:`，持久 Executor
用 `E1:`，fresh Reviewer 用 `R1:`／`R2:`，接管用 `E2:`。派工、ready 和結果
回執都要帶可核實 session／thread id 或平台等價座標，避免只發 prompt 卻斷掉
回傳閉環。

E1／R 完成或受阻後主動 direct-push 給 C。C 不靠 waiting、輪詢或背景監聽找
成果；收到 push 後才做有界讀回與裁決。

## 知識底座

CER 不只服務工程任務。醫療、法律、金融、投資、政策、學術、商業、設計、
營運或內容工作，只要依賴專業知識，C 就要先界定任務知識底座：領域範圍、
權威來源、術語、資料年度、品質標準、未知和不可由 AI 代選的取捨。E1 按這個
範圍執行；R 按同一範圍做獨立反證。簡單任務不因此新增文件。

## 實際使用

使用者輸入 `CER 工作法啟動：<總任務>`。C 顯示目標、現有真源、目前階段、
角色狀態及下一停點，然後建立或復用同一 E1 分批執行。

重大方向、交付形狀、成本或範圍未裁決時，C 在主 task 停下讓使用者回饋；
普通工程細節由 AI 自行閉環。可觀察階段成果與最終交付會再次呈現。

CER 不要求特定模型版本。模型與力度按使用者限制、平台可用性、成本和風險
選擇；未能讀回型號時不可假稱已確認，也不因此自行阻塞。

## 守 YAGNI 原則

CER 的角色、批次、Reviewer、停點和驗收按風險比例化。不因小修改建立 R，
不以更多代理、文件、審閱或治理儀式代替清晰目標與可驗收條件。高風險修補
只重審受影響邊界；治理成本壓過任務價值時必須縮減協作結構。

## 可選的項目真相工作法

若使用者要把模糊構思循環收斂成藍圖、需求、按需 R&D、共識計劃及可視進度，
可另用 `skills/project-context-workflow/`。它可以單獨使用，也可把已確認真源
交給 CER 執行；任何一方都不是另一方的安裝前置。

項目脈絡工作法先沿用既有 PRD、Roadmap、Issue、研究及進度來源。只有全新、
模糊、複雜或長期項目沒有適合真源時，才按需建立五份模板。藍圖容許願景與
探索；YAGNI 限制的是未經裁決的升格、承諾與實作，不壓縮創意。

## CER v2

v2 指 Agent Handoff Kit Adapter。它處理 Kit recovery、Persistence Gate、
handoff/log、START mirror、closeout-status 及新 session 恢復。v2 不是 v1 的
隱藏能力；必須另行證明 task 建立、送訊、回執、writer 停止、Kit closeout
及新 session 恢復全鏈路。只證明標題、fork、單向送訊或文件格式不足以通過。

## 四色熊卡與路線圖

| 顏色 | 場景 |
|---|---|
| 🔵 計劃預覽 | 把既有 plan／progress 翻譯成即將執行的方向、交付與下一停點。 |
| 🟡 方向抉擇 | 重大方向、範圍、使用方式、知識取捨或交付形狀需要使用者裁決。 |
| 🔴 重大阻礙 | 缺真源、知識底座、權限、平台能力或安全條件，不能可靠繼續。 |
| 🟢 階段性交付／最終驗收 | 已有可觀察成果、需要回饋或準備收工。 |

Codex 支援時，路線圖預設使用真正 inline visualization。Mermaid 只在 inline
不可用或呈現失敗後降級使用；再不可用才退回固定文字，並明示降級原因。

小熊卡和 inline visualization 只在啟動、首批前、方向裁決、重大阻礙、階段
交付和收工時真實使用。E1 的普通小步驟、C 的內部讀檔或明確無需使用者介入的
下一步不發卡。
