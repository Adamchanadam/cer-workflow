interface:
  display_name: "CER Workflow"
  short_description: "Run long work through a C, E1, and R closed loop"
  default_prompt: "Use $cer-workflow-en to run this work through C, E1, and R with direct cross-task return."
